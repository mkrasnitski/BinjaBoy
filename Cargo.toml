[package]
name = "binja-boy"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
binaryninja = { path = "../binaryninja-api/rust" }
enum-primitive-derive = "^0.1"
tracing = "0.1"
num-traits = "^0.1"
